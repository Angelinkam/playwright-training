import { test, expect } from '@playwright/test';

test.describe("Test suite for update user", () =>{
    test.beforeEach("Navigate to login", async({page}) => {
        await page.goto('/');
    }
    
    )

    test.describe("Test suite for update username", () =>{


test('test for update username with valid username', async ({ page }) => {

});

test('test for update username with invalid username', async ({ page }) => {

});

test('test for update username with empty username', async ({ page }) => {

});
});

test.describe("Test suite for update user password", () =>{

test('test for update password', async ({ page }) => {
   
});



});

test.afterEach("tear down for out tests", async({page}) => {

});
});